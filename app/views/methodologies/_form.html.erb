<%= form_with(model: methodology, local: true) do |form| %>
  <% if methodology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(methodology.errors.count, "error") %> prohibited this methodology from being saved:</h2>

      <ul>
      <% methodology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :methodology_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :methodology_description %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :methodology_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
