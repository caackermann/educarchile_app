<%= form_with(model: project_review, local: true) do |form| %>
  <% if project_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_review.errors.count, "error") %> prohibited this project_review from being saved:</h2>

      <ul>
      <% project_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :stars %>
    <%= form.number_field :stars, id: :project_review_stars %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :project_review_description %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :project_review_user_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :project_review_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
