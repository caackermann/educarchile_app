<%= simple_form_for([@project, @project.project_planification]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

  <div class="form-inputs">
    <%= f.input :place %>
  </div>

  <div class="form-inputs">
    <%= f.datetime_select :startdate %>
  </div>

  <div class="form-inputs">
    <%= f.datetime_select :finishdate %>
  </div>

  <h2>Recursos Físicos y Financieros</h2>

  <table class="table">
    <thead>
      <tr>
        <th></th>
        <th>Item</th>
        <th>Disponible </th>
        <th>¿Cómo podemos obtener los recursos?</th>
      </tr>
    </thead>

    <tbody class ="project_resources">
      <%= f.simple_fields_for :project_resources do |builder| %>
      <%= render partial: 'project_resource_fields', f: builder %>
      <% end %>
    </tbody>
  </table>

  <div class="form-actions">
    <%= link_to_add_association 'Agregar Recurso', f, :project_resources, class: 'btn btn-primary', data: { association_insertion_node: '.project_resources', association_insertion_method: :append } %>
  </div>

  <h3>Otras condiciones necesarias <br> (autorizaciones, ajuste de horarios, disponibilidad de espacios, entre otros)</h3>

  <table class="table">
    <thead>
      <tr>
        <th></th>
        <th>Item</th>
        <th>¿Cómo podemos obtener los recursos?</th>
      </tr>
    </thead>

    <tbody class ="project_conditions">
      <%= f.simple_fields_for :project_conditions do |builder| %>
      <%= render partial: 'project_condition_fields', f: builder %>
      <% end %>
    </tbody>
  </table>

  <div class="form-actions">
    <%= link_to_add_association 'Agregar Recurso', f, :project_conditions, class: 'btn btn-primary', data: { association_insertion_node: '.project_conditions', association_insertion_method: :append } %>
  </div>





<div class="form-actions">
    <%= f.button :submit %>

</div>

<% end %>
